Compare list `==` and `!=`
- [ ] create internal function compare list
- [ ] recursively call comparelist if list in list

Compare string `==` and `!=`
- [ ] create internal function lenght string
- [ ] create internal function compare string

For internal function:
1. create internal function in `comparison.rs`
2. add it in `internal_function.rs` file in enum
3. impl into
4. and call it in `init_internal_functions`
